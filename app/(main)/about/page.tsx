import AboutContent from '@/components/sections/AboutContent'

export default function AboutPage() {
  return (
    <main className="h-screen flex items-center w-full overflow-hidden">
      <AboutContent />
    </main>
  )
}
